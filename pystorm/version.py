"""pystorm version information. We use semantic versioning (see http://semver.org/)."""

name = "pystorm"
version_info = (1, 0, 0)  # (major, minor, patch)

version = "{v}".format(v='.'.join(str(v) for v in version_info))
